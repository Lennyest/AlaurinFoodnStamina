<project name="MasteryProject" default="Build">
    <!--

           Get all classes from target/classes in your plugin code folder,
           compile them and place in your test server folder automatically.

   -->
    <target name="Build">
        <jar jarfile="F:\Development\Servers\1.12 server\plugins\${ant.project.name}.jar" basedir="./target/classes/" includes="**/*"/>
    </target>
</project>